import React from 'react';
import PropTypes from 'prop-types';

const Posting = props => {
    return (
        <div style={{width:'80%'}}>
            글싸기 페이지
        </div>
    );
};

Posting.propTypes = {
    
};

export default Posting;