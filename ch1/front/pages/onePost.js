import React from 'react';
import PropTypes from 'prop-types';

const onePost = (  ) => {
    return (
        <div>
            
        </div>
    );
};

onePost.propTypes = {
    
};

export default onePost;